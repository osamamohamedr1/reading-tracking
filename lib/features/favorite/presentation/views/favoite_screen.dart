import 'package:flutter/material.dart';

class FavoiteScreen extends StatelessWidget {
  const FavoiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Favoite'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
